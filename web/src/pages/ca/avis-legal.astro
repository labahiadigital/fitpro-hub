---
const appUrl = import.meta.env.PUBLIC_APP_URL || 'https://app.trackfiz.com';
const loginUrl = `${appUrl}/login`;
---
<!doctype html>
<html lang="ca">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="refresh" content={`0;url=${loginUrl}`} />
  <link rel="canonical" href={loginUrl} />
  <title>Redirigint...</title>
</head>
<body>
  <p>Redirigint a <a href={loginUrl}>login</a>...</p>
</body>
</html>
