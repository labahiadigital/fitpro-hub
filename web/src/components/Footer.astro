---
import { getLangFromUrl, useTranslations, useTranslatedPath, getLocalizedRoute } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const translatePath = useTranslatedPath(lang);

const productLinks = [
  { label: t('footer.product.features'), href: '#features' },
  { label: t('footer.product.pricing'), href: '#pricing' },
  { label: t('footer.product.integrations'), href: '/integraciones' },
  { label: t('footer.product.api'), href: '/api' },
];

const resourceLinks = [
  { label: t('footer.resources.blog'), href: '/blog' },
  { label: t('footer.resources.guides'), href: '/guias' },
  { label: t('footer.resources.webinars'), href: '/webinars' },
  { label: t('footer.resources.help'), href: '/ayuda' },
];

const legalLinks = [
  { label: t('footer.legal.notice'), href: translatePath(`/${getLocalizedRoute('aviso-legal', lang)}`) },
  { label: t('footer.legal.privacy'), href: translatePath(`/${getLocalizedRoute('privacidad', lang)}`) },
  { label: t('footer.legal.cookies'), href: translatePath(`/${getLocalizedRoute('cookies', lang)}`) },
  { label: t('footer.legal.terms'), href: translatePath(`/${getLocalizedRoute('politica-devoluciones', lang)}`) },
];

const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="container">
    <div class="footer-grid">
      <div class="footer-brand">
        <a href={translatePath('/')} class="logo">
          <div class="logo-icon">T</div>
          <span class="logo-text">Trackfiz</span>
        </a>
        <p class="footer-description">
          {t('footer.description')}
        </p>
        <div class="app-badges">
          <a href="#" class="app-badge" aria-label="iOS App">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20.94c1.5 0 2.75 1.06 4 1.06 3 0 6-8 6-12.22A4.91 4.91 0 0 0 17 5c-2.22 0-4 1.44-5 2-1-.56-2.78-2-5-2a4.9 4.9 0 0 0-5 4.78C2 14 5 22 8 22c1.25 0 2.5-1.06 4-1.06Z"/><path d="M10 2c1 .5 2 2 2 5"/></svg>
          </a>
          <a href="#" class="app-badge" aria-label="Android App">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 10v10a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1V10"/><path d="M15 5l2-3"/><path d="M9 5l-2-3"/><rect x="4" y="5" width="16" height="5" rx="1"/></svg>
          </a>
        </div>
      </div>
      
      <nav class="footer-links" aria-label={t('footer.product')}>
        <p class="footer-links-title">{t('footer.product')}</p>
        <ul>
          {productLinks.map(link => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </nav>
      
      <nav class="footer-links" aria-label={t('footer.resources')}>
        <p class="footer-links-title">{t('footer.resources')}</p>
        <ul>
          {resourceLinks.map(link => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </nav>
      
      <nav class="footer-links" aria-label={t('footer.legal')}>
        <p class="footer-links-title">{t('footer.legal')}</p>
        <ul>
          {legalLinks.map(link => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </nav>
    </div>
    
    <div class="footer-bottom">
      <p>&copy; {currentYear} {t('footer.copyright')}</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: #0a0a14;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    padding-block: 4rem;
  }
  
  .footer-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: 3rem;
  }
  
  @media (max-width: 1024px) {
    .footer-grid {
      grid-template-columns: 1fr 1fr;
    }
    
    .footer-brand {
      grid-column: span 2;
    }
  }
  
  @media (max-width: 640px) {
    .footer-grid {
      grid-template-columns: 1fr;
    }
    
    .footer-brand {
      grid-column: span 1;
    }
  }
  
  .logo {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }
  
  .logo-icon {
    width: 32px;
    height: 32px;
    background: linear-gradient(135deg, var(--color-accent) 0%, var(--color-accent-light) 100%);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 14px;
    color: var(--color-background);
  }
  
  .logo-text {
    font-weight: 700;
    font-size: 1.125rem;
    color: var(--color-text);
  }
  
  .footer-description {
    color: var(--color-text-muted);
    font-size: 0.875rem;
    line-height: 1.6;
    margin-bottom: 1rem;
  }
  
  .app-badges {
    display: flex;
    gap: 0.75rem;
  }
  
  .app-badge {
    width: 36px;
    height: 36px;
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.05);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-text);
    transition: background 0.2s;
  }
  
  .app-badge:hover {
    background: rgba(255, 255, 255, 0.1);
  }
  
  .footer-links-title {
    font-weight: 600;
    margin-bottom: 1rem;
    color: var(--color-text);
  }
  
  .footer-links ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  
  .footer-links a {
    color: var(--color-text-muted);
    font-size: 0.875rem;
    transition: color 0.2s;
  }
  
  .footer-links a:hover {
    color: var(--color-text);
  }
  
  .footer-bottom {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    text-align: center;
  }
  
  .footer-bottom p {
    color: #9ca3af;
    font-size: 0.875rem;
  }
</style>
