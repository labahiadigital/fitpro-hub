fix: respuestas de formularios, onboarding idempotente, reset contrasena y chips de ejercicio

- FormsPage: ver/descargar respuesta, marcar automaticamente como leida al abrir,
  estado "Nueva" (rojo) para submissions recien enviadas.
- ClientDetailPage: mover useMemo de actividades antes de early returns para evitar
  Minified React error #310; anadir opcion "Enviar email para restablecer contrasena"
  en el menu de acciones del cliente.
- InvitationOnboardingPage: bloquear dobles envios cuando loading/completed.
- /invitations/complete/{token} idempotente: si la invitacion ya fue aceptada y el
  usuario existe, devolvemos success silencioso en lugar de error.
- /auth/forgot-password: no falla si el envio de email cae; se loggea y se responde
  siempre con mensaje generico.
- Nuevo endpoint /clients/{id}/send-password-reset para que el entrenador envie el
  email de restablecimiento, usando la plantilla con marca del workspace.
- MyWorkoutsPage: helper getExercisePrescriptionChips que muestra series x reps,
  peso, duracion, distancia, km/h y descanso reales en lugar de valores genericos.
